syntax = "proto3";

enum MessageType {
   REQUEST = 0;
   RESPONSE = 1;
}

message Header {
    required uint32 type;
    required uint32 cmd;
}

message ExtraHeader {
    
}

message Message {
    required Header header;
    optional ExtraHeader extra_header;
    required bytes payload;
}
